<h1>Editing Photo</h1>
<div class="container">
<%= form_for([@item,@photo], html: {multipart: true}) do |f| %>
  <% if @photo.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

        <ul>
          <% @photo.errors.full_messages.each do |message| %>
              <li><%= message %></li>
          <% end %>
        </ul>
      </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :Featured %>
    <%= f.check_box :featured %>
  </div>

  <div class="actions">
    <%= f.submit("Update") %>
  </div>
<% end %>


<%= link_to 'Back', item_photos_path(@item) %>
